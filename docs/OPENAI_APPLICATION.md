# OpenAI Application Draft

## Repository summary

`DiffDocket` is a public OSS maintainer tool that turns a pull request into a
structured review packet. It highlights changed files, classifies risk, points out
missing tests, and drafts a release note. The core CLI is deterministic and
local-first; future API use is optional and scoped to reviewer assistance.

Repository: https://github.com/LarryLemonBot/diffdocket

Hosted page: https://diffdocket.vercel.app

## Why this repository qualifies

This repository directly supports maintainer workflows: pull request review, issue
triage, and release preparation. It is deterministic, public, and designed for repeated
use across open-source projects.

Draft answer for the form field:

> `DiffDocket` is a public maintainer tool that turns pull requests into structured review receipts with changed files, risk areas, test gaps, and release-note drafts. It supports active maintenance workflows for GitHub OSS projects, especially small projects where maintainers need fast, repeatable review context before merging.

## How API credits would be used

Draft answer for the form field:

> API credits would add optional AI summaries for diff risk, reviewer guidance, and release-note drafting while keeping the core analyzer deterministic and local-first. The credits would help build a reviewer assistant layer that explains risky changes, recommends test coverage, and turns the deterministic receipt into concise maintainer guidance.

## Anything else

Draft answer for the form field:

> DiffDocket is intentionally narrow, public, and maintainable. It is meant to reduce maintainer load without becoming a generic chatbot wrapper. The repository includes CI, Dependabot, CodeQL, OpenSSF Scorecard, a security policy, a release, and a public hosted page so reviewers can inspect the project quickly.

## Fields still needed for submission

- First name
- Last name
- Email associated with the ChatGPT account
- OpenAI Organization ID
